@using ERPv1.ERP.SalesModule.Model

@model ClientPaymentMethod
@{
    var options = Infrastructure.Extensions.EnumExtension.GetEnumSelectList<ClientPaymentMethod>()
        .Where(x=>x.Value != "10").ToList();
}
@{
    var htmlAttributes = ViewData["htmlAttributes"] ?? new { };
}

@Html.DropDownList("", options, htmlAttributes)