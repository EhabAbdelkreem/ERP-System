@using ERPv1.ERP.PurchasesModule.Model

@model SupplierPaymentMethod
@{
    var options = Infrastructure.Extensions.EnumExtension.GetEnumSelectList<SupplierPaymentMethod>()
        .Where(x=>x.Value != "10").ToList();
}
@{
    var htmlAttributes = ViewData["htmlAttributes"] ?? new { };
}

@Html.DropDownList("", options, htmlAttributes)